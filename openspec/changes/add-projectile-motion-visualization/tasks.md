# Tasks: 平抛运动可视化实现

## 阶段 1：基础设施搭建

- [x] 1.1 安装 p5.js 依赖
  - 运行 `npm install p5 --save`
  - 验证 package.json 中版本为 ^1.9.0

- [x] 1.2 创建物理计算模块 `src/utils/physic.js`
  - 实现 `calculatePosition(v0, g, h, theta, t)` 函数
  - 实现 `calculateVelocity(v0, g, theta, t)` 函数
  - 实现 `predictLandingTime(v0, g, h, theta)` 函数
  - 实现 `calculatePeakPoint(v0, g, h, theta)` 函数
  - 实现 `validateParams(params)` 函数
  - 添加完整中文注释说明物理公式和坐标系

- [x] 1.3 扩展全局状态 `src/store/index.js`
  - 添加 `projectileParams` 对象（包含 v0, g, h, theta, mass, timeScale）
  - 添加 `updateProjectileParams(params)` 方法
  - 导出新增的状态和方法

## 阶段 2：核心可视化组件

- [x] 2.1 创建 `src/components/ProjectileMotion.vue` 基础结构
  - 创建组件模板（参数控制区 + 画布挂载点 + 动画控制区 + 公式展示区）
  - 定义 props 接收初始参数（v0, g, h, theta）
  - 定义 reactive 状态（params, animationState, keyPoints）

- [x] 2.2 实现 p5.js 初始化逻辑
  - 在 `onMounted` 中创建 p5 实例
  - 实现 `sketch.setup()` 函数（创建画布、设置帧率、计算缩放比例）
  - 实现 `sketch.windowResized()` 函数（响应窗口缩放）
  - 在 `onUnmounted` 中调用 `p5Instance.remove()` 清理资源

- [x] 2.3 实现坐标系转换函数
  - 实现 `physicsToCanvas(x, y)` 函数
  - 实现 `canvasToPhysics(x, y)` 函数
  - 添加动态缩放计算逻辑（确保初始高度占画布 70%）

- [x] 2.4 实现基础绘制函数
  - 实现 `drawObject(p)` 绘制运动物体（墨绿色实心圆）
  - 实现 `drawGuides(p)` 绘制辅助线（地面线、初始位置虚线）
  - 实现 `drawTrajectory(p)` 绘制渐变轨迹（深绿到浅灰）
  - 实现 `drawKeyPoints(p)` 绘制关键节点（初始、最高点、落地）

- [x] 2.5 实现动画循环逻辑
  - 在 `sketch.draw()` 中实现时间更新逻辑
  - 实现轨迹点采样和存储（根据倍速调整采样间隔）
  - 实现落地检测和自动停止
  - 调用绘制函数渲染画面

## 阶段 3：参数控制 UI

- [x] 3.1 实现参数输入控件
  - 添加初速度 v0 滑块（1-50 m/s）+ 数值输入框
  - 添加重力加速度 g 滑块（1-20 m/s²）+ 数值输入框
  - 添加初始高度 h 滑块（10-200 m）+ 数值输入框
  - 添加发射角度 θ 滑块（0-90°）+ 数值输入框
  - 添加质量 m 只读显示（默认 1 kg）

- [x] 3.2 实现参数双向绑定
  - 使用 `v-model` 绑定滑块和输入框到 `params` 对象
  - 添加输入验证（范围限制、数值格式）
  - 实现滑块和输入框的同步更新

- [x] 3.3 实现参数变化监听
  - 使用 `watch` 监听 params 变化
  - 实现参数更新策略（播放时暂停并重置，停止时重新计算）
  - 调用 `recalculateKeyPoints()` 更新关键点
  - 同步参数到全局状态 `state.projectileParams`

- [x] 3.4 添加参数控制样式
  - 使用 A2UI 配色（墨绿色主题）
  - 滑块样式：轨道 #e0e0e0，滑块 #006644
  - 输入框样式：边框 #e0e0e0，聚焦时边框 #006644
  - 标签样式：14px 字体，#333 颜色

## 阶段 4：动画控制功能

- [x] 4.1 实现播放/暂停按钮
  - 添加按钮 UI（图标 + 文字）
  - 实现 `togglePlayPause()` 方法切换 `animationState.isPlaying`
  - 按钮状态同步（播放时显示"暂停"，暂停时显示"播放"）

- [x] 4.2 实现重置按钮
  - 添加重置按钮 UI
  - 实现 `resetAnimation()` 方法（清空轨迹、重置时间、恢复默认参数）
  - 重置后自动停止动画

- [x] 4.3 实现倍速调节按钮
  - 添加倍速按钮组（0.5x / 1x / 2x / 3x）
  - 实现倍速切换逻辑（更新 `params.timeScale`）
  - 高亮当前倍速按钮

- [x] 4.4 实现单步前进按钮
  - 添加单步前进按钮 UI
  - 实现 `stepForward()` 方法（暂停状态下前进 0.1 秒）
  - 更新轨迹和参数显示
  - 播放状态下禁用单步按钮

- [x] 4.5 添加动画控制样式
  - 按钮组水平排列，间距 8px
  - 主按钮样式：背景 #006644，文字白色
  - 次要按钮样式：背景 #f5f5f5，文字 #333
  - hover 和 active 状态样式

## 阶段 5：交互功能

- [x] 5.1 实现鼠标拖拽物体
  - 在 `sketch.mousePressed()` 中检测点击物体（距离 < 20px）
  - 设置 `animationState.isDragging = true`
  - 在 `sketch.mouseDragged()` 中更新物体位置（转换为物理坐标）
  - 限制拖拽范围（h: 10-200m）

- [x] 5.2 实现点击暂停/播放
  - 在 `sketch.mousePressed()` 中检测点击空白区域
  - 调用 `togglePlayPause()` 切换播放状态
  - 避免拖拽松开时触发点击（使用 `isDragging` 标志位）

- [x] 5.3 实现拖拽与点击冲突处理
  - 在 `sketch.mouseReleased()` 中检查 `isDragging` 状态
  - 拖拽松开时不触发点击事件（提前 return）
  - 重置 `isDragging` 和 `dragTarget` 状态

- [x] 5.4 添加交互视觉反馈
  - 鼠标悬停物体时显示手型光标
  - 拖拽时物体高亮（边框或阴影）
  - 拖拽时显示当前高度数值

## 阶段 6：公式与结果展示

- [x] 6.1 创建公式展示区域
  - 在组件底部添加公式容器 `<div id="formula-display">`
  - 分为两部分：核心公式区 + 实时结果区
  - 使用 flex 布局，左右各占 50%

- [x] 6.2 实现公式内容生成
  - 创建 `formulaContent` computed 属性
  - 生成核心公式 LaTeX 字符串（位移、速度公式）
  - 生成实时结果 HTML（当前时间、位移、速度、落地预测）

- [x] 6.3 实现 KaTeX 渲染
  - 使用 `watch` 监听 `formulaContent` 变化
  - 在 `nextTick` 中调用 `renderMathInElement()` 渲染公式
  - 确保公式样式与现有公式统一（块级公式居中、字号更大）

- [x] 6.4 实现实时结果更新
  - 在动画循环中计算当前位置和速度
  - 更新 `formulaContent` 触发重新渲染
  - 数值保留 2 位小数，加粗显示

- [x] 6.5 添加公式展示样式
  - 公式区域背景 #fff，边框 #e0e0e0，圆角 4px
  - 标题样式：16px 字体，#006644 颜色，加粗
  - 公式块级样式：居中对齐，字号 18px
  - 结果文字样式：14px 字体，数值加粗 #006644

## 阶段 7：集成到主应用

- [x] 7.1 在 VisualCanvas.vue 中引入 ProjectileMotion 组件
  - 导入 `ProjectileMotion.vue`
  - 在模板中添加条件渲染（`v-if="state.currentVisualType === 'PROJECTILE'"`)
  - 传递初始参数 props（从 `state.projectileParams` 读取）

- [x] 7.2 更新 VisualCanvas.vue 的 updateCanvas 方法
  - 在 `type === 'PROJECTILE'` 分支中初始化 ProjectileMotion 组件
  - 更新状态栏文字为"当前模式 - 平抛运动"
  - 移除占位提示文字（由 ProjectileMotion 组件接管）

- [ ] 7.3 测试对话联动
  - 在 ChatBox.vue 中添加关键词检测逻辑（"平抛运动"、"初速度 Xm/s"）
  - 提取参数并更新 `state.projectileParams`
  - 设置 `state.currentVisualType = 'PROJECTILE'`
  - 验证画布自动加载并播放动画

## 阶段 8：样式与优化

- [x] 8.1 创建 ProjectileMotion.vue 样式文件
  - 创建 `src/assets/ProjectileMotion.css`
  - 定义 A2UI 配色变量（墨绿色主题）
  - 实现参数控制区、动画控制区、公式展示区样式
  - 确保样式与现有组件一致

- [x] 8.2 实现性能优化
  - 限制轨迹点数量（最多 1000 个点）
  - 根据倍速调整采样间隔（避免点位过密）
  - 停止状态下减少重绘频率（仅参数变化时重绘）

- [x] 8.3 实现响应式布局
  - 参数控制区使用 grid 布局（2 列）
  - 动画控制按钮使用 flex 布局（水平排列）
  - 公式展示区使用 flex 布局（左右分栏）
  - 确保画布自适应右侧区域（70% 宽度，100% 高度）

- [ ] 8.4 添加加载状态和错误处理
  - p5.js 加载失败时显示错误提示
  - 参数验证失败时显示错误信息（红色文字）
  - 画布初始化失败时显示占位提示

## 阶段 9：测试与验证

- [ ] 9.1 物理计算准确性测试
  - 测试纯平抛（θ=0°）：验证 x = v₀t，y = h - ½gt²
  - 测试斜抛（θ=45°）：验证最大射程公式
  - 测试边界条件：h=0、v₀=0、θ=90°
  - 对比理论值和计算值，误差 < 1%

- [ ] 9.2 交互功能测试
  - 测试拖拽物体：验证高度更新、轨迹重算、不触发点击
  - 测试点击暂停/播放：验证状态切换、按钮文字更新
  - 测试参数修改：验证播放时自动暂停、停止时重新计算
  - 测试单步前进：验证暂停状态下前进 0.1 秒、播放时禁用

- [ ] 9.3 动画流畅性测试
  - 使用 Chrome DevTools Performance 监控帧率
  - 验证 60fps 稳定性（长时间运行 10 分钟）
  - 验证参数修改响应时间 < 100ms
  - 验证窗口缩放后画布正确适配

- [ ] 9.4 视觉一致性测试
  - 使用取色器验证关键元素颜色（物体 #006644、轨迹渐变、背景 #F5F5F5）
  - 验证公式样式与现有公式统一（块级居中、字号 18px）
  - 验证按钮样式符合 A2UI 规范（hover/active 状态）
  - 截图对比：初始状态、播放中、暂停、落地

- [ ] 9.5 对话联动测试
  - 测试关键词触发："平抛运动" → 自动加载默认参数
  - 测试参数提取："初速度 20m/s 的平抛运动" → v0=20
  - 测试角度提取："45 度角抛出" → θ=45
  - 验证画布自动播放动画

- [ ] 9.6 兼容性测试
  - 验证不破坏现有功能（流式输出、语音输入、公式渲染）
  - 验证切换到其他可视化类型（FORMULA）后正常工作
  - 验证重置画布后 p5 实例正确清理（无内存泄漏）

## 阶段 10：文档与收尾

- [ ] 10.1 添加代码注释
  - 在 ProjectileMotion.vue 中添加完整中文注释
  - 在 physic.js 中添加物理公式说明和坐标系说明
  - 在关键函数中添加参数说明和返回值说明

- [ ] 10.2 更新项目文档
  - 更新 README.md（如果存在）添加平抛运动功能说明
  - 更新 openspec/project.md 添加 p5.js 依赖说明

- [ ] 10.3 最终验证
  - 运行 `npm run dev` 启动项目
  - 完整测试所有功能（参数调节、动画控制、交互、对话联动）
  - 确认无控制台错误或警告
  - 确认性能指标达标（60fps、响应时间 < 100ms）

## 验收标准

- ✅ 所有 6 个参数可调节，范围和默认值正确
- ✅ 所有 5 个动画控制按钮正常工作（播放/暂停、重置、倍速、单步）
- ✅ 鼠标拖拽物体修改高度，点击空白区域暂停/播放
- ✅ 轨迹渐变显示（深绿到浅灰），关键节点标注清晰
- ✅ 公式使用 KaTeX 渲染，样式与现有公式统一
- ✅ 实时结果准确显示（时间、位移、速度、落地预测）
- ✅ 物理计算准确性误差 < 1%
- ✅ 60fps 动画无卡顿，参数修改响应时间 < 100ms
- ✅ 配色符合 A2UI 规范（墨绿色主题）
- ✅ 对话联动正常（关键词触发、参数提取、自动播放）
- ✅ 不破坏现有功能（流式输出、语音输入、公式渲染）
- ✅ 窗口缩放后画布正确适配
- ✅ 组件卸载后 p5 实例正确清理（无内存泄漏）
