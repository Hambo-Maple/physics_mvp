# UI 界面描述总结

## 📋 文档已生成

我已经为您创建了一份详细的 UI 界面规范文档，专门为 Figma 设计师准备。

**文档位置**: `UI_SPECIFICATION_FOR_FIGMA.md`
**文档长度**: 596 行
**格式**: Markdown (Figma 可读)

---

## 📐 核心内容概览

### 1. 整体布局架构
- **主容器**: 全屏 Flexbox 水平分割布局
- **左侧面板 (ChatBox)**: 30% 宽度，垂直 Flex 布局
- **右侧面板 (VisualCanvas)**: 70% 宽度，垂直 Flex 布局

### 2. 设计系统 (Design Tokens)
- **颜色系统**: 完整的 HSL 颜色变量定义
  - 9 种主要颜色 (Background, Foreground, Primary, Secondary, Muted, Accent, Destructive, Border, Ring)
  - 支持 Light Mode 和 Dark Mode
  - 所有颜色都有对应的 HEX 值
- **圆角系统**: 3 种尺寸 (8px, 6px, 4px)
- **间距系统**: 基于 4px 倍数的 Tailwind 间距
- **字体系统**: 3 种字号 (12px, 14px, 18px) + 2 种字重

### 3. 左侧面板详细规范 (ChatBox)
包含 5 个主要区域：
1. **Toast 通知**: 固定在右上角，成功/错误两种状态
2. **标题栏**: "物理可视化助手"，带底部边框
3. **连续语音状态栏**: 条件显示，包含波形动画
4. **消息流区域**: 自定义滚动条，用户消息右对齐，AI 消息左对齐
5. **输入区域**: Textarea + 3 个按钮（语音输入、连续对话、发送）

### 4. 右侧面板详细规范 (VisualCanvas)
包含 2 种视图：
1. **平抛运动组件**: p5.js 物理仿真画布
2. **默认视图**: 状态栏 + 占位区域 + 控制按钮

### 5. UI 组件库规范
详细定义了 6 个 shadcn-vue 组件：
- **Button**: 6 种变体 × 4 种尺寸 = 24 种组合
- **Textarea**: 完整的状态样式（默认、悬停、焦点、禁用、错误）
- **Badge**: 4 种变体
- **ScrollArea**: 自定义滚动条样式
- **Card**: 卡片容器
- **Separator**: 分隔线

### 6. 交互状态规范
- **按钮状态**: 5 种（默认、悬停、激活、焦点、禁用）
- **输入框状态**: 5 种（默认、悬停、焦点、禁用、错误）
- **消息状态**: 3 种（发送中、生成中、完成）
- **语音状态**: 4 种（空闲、录音中、识别中、连续模式）

### 7. 尺寸参考表
提供了所有关键元素的精确尺寸：
- 宽度、高度、内边距、圆角
- 11 种常用元素的完整尺寸数据

### 8. Figma 导入建议
- **颜色变量映射**: 9 种颜色的 HEX 值
- **组件创建顺序**: 9 步完整流程
- **Auto Layout 设置**: 主容器、面板、列表、按钮组的布局配置

---

## 🎯 文档特点

### ✅ 为 Figma 设计师优化
1. **精确的尺寸数据**: 所有元素都有 px 值
2. **完整的颜色映射**: HSL + HEX 双格式
3. **详细的组件规范**: 每个组件的所有变体和状态
4. **Auto Layout 指导**: 直接可用的布局配置
5. **验收清单**: 14 项检查点

### ✅ 结构清晰
- 使用 Markdown 格式，易于阅读
- 分层级的标题结构
- 表格展示尺寸数据
- 代码块展示技术细节

### ✅ 内容全面
- 596 行详细规范
- 涵盖布局、颜色、组件、交互、可访问性
- 包含响应式建议和设备兼容性

---

## 📖 如何使用这份文档

### 给 Figma 设计师
1. 打开 `UI_SPECIFICATION_FOR_FIGMA.md`
2. 按照"Figma 导入建议"章节创建颜色变量
3. 按照"组件创建顺序"逐步构建组件
4. 参考"尺寸参考表"设置精确尺寸
5. 使用"Auto Layout 设置"配置布局
6. 对照"验收清单"检查完成度

### 给开发者
1. 参考"设计系统"章节了解 CSS 变量
2. 查看"UI 组件库规范"了解组件 API
3. 参考"交互状态规范"实现状态逻辑
4. 使用"技术实现细节"章节的代码示例

---

## 📊 文档统计

- **总行数**: 596 行
- **章节数**: 9 个主要章节
- **组件数**: 6 个 UI 组件
- **颜色变量**: 9 种主要颜色
- **按钮变体**: 6 种
- **交互状态**: 17 种
- **尺寸数据**: 11 个元素

---

## ✨ 核心亮点

1. **1:1 还原**: 文档完全基于当前实现的代码
2. **双向兼容**: 既适合 Figma 设计，也适合代码开发
3. **精确数据**: 所有尺寸、颜色、间距都有精确值
4. **完整覆盖**: 从布局到组件到交互到可访问性
5. **实用导向**: 包含 Figma 导入步骤和验收清单

---

**现在您可以将这份文档交给 Figma 设计师，他们可以根据这份规范在 Figma 中完整还原您的 UI 界面！**
